<h1>投稿一覧</h1>

<%= form_with model: @post, local: true do |form| %>
  <div>
    <%= form.label :body, "投稿" %>
    <%= form.text_field :body %>
  </div>
  <div>
    <%= form.submit "送信" %>
  </div>
<% end %>

<p>
  <% @posts.each do |post| %>
    <%= post.body %>
  <%= end %>
</p>
